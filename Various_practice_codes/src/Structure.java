public interface Structure {
	//acts as an outline
	//anything that implements this needs to have these functions
	public void push (int item);
	public int pop();
	public String toString();
	public int top();
	
}